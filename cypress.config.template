const { defineConfig } = require('cypress');

module.exports = defineConfig({
  e2e: {
    env: {
      BASE_URL: '##!!BASE_URL!!##',
    },
    viewportWidth: 1200,
    retries: {
      runMode: 2,
      openMode: 0,
    },
  },
});
